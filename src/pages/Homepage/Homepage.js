import { HomepageContainer, Logo, LogoContainer } from "./styled";

/**
 *
 */
function Homepage() {
    return (
        <HomepageContainer>
            <LogoContainer>
                <Logo />
                <span>LeafCraft</span>
            </LogoContainer>
        </HomepageContainer>
    );
}

export default Homepage;
